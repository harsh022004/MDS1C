# AGENTS.md

## Purpose
This file provides guidance for AI coding agents working on this project. It outlines the project's structure, conventions, and key workflows to ensure agents can assist effectively.

## Project Overview
- **Main Language**: Python
- **Key Files**:
  - `main.py`: Entry point of the application.
  - `index.html` and `results.html`: HTML files for the web interface.
  - `requirements.txt`: Lists Python dependencies.
  - `app.yaml`: Configuration file (possibly for deployment).

## Development Guidelines
1. **Python Environment**:
   - Ensure all dependencies in `requirements.txt` are installed.
   - Use `pip install -r requirements.txt` to set up the environment.

2. **Code Structure**:
   - `main.py` likely contains the core logic. Follow Python best practices for readability and maintainability.

3. **HTML Files**:
   - `index.html` and `results.html` are part of the user interface. Ensure changes align with the application's purpose.

4. **Configuration**:
   - `app.yaml` may define deployment settings. Verify its syntax and purpose before modifying.

## Common Tasks
- **Run the Application**:
  - Use `python main.py` to execute the application.
- **Update Dependencies**:
  - Modify `requirements.txt` and run `pip install -r requirements.txt`.

## Notes for AI Agents
- Follow Python and HTML best practices.
- Ensure compatibility with the existing project structure.
- Validate changes to `app.yaml` to avoid deployment issues.

## Additional Resources
- Add links to external documentation or resources here if needed.